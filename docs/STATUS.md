# Implementation status

Core production slice implemented on `feature/ai-digital-twin`.

Pending before live launch:

- add `OPENAI_API_KEY` and AI model environment variables in Vercel;
- validate CI / build;
- index verified sources through `/admin/twin`;
- add native PDF/DOCX ingestion;
- add Realtime voice in the next phase.
